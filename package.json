{
    "name": "expense-tracker-root",
    "private": true,
    "description": "Root package.json for Render deployment",
    "scripts": {
        "install-all": "npm install --prefix client && npm install --prefix node-backend"
    }
}